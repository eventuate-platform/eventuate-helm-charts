apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "authorization-server.fullname" . }}-test-connection"
  labels:
    {{- include "authorization-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "authorization-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
